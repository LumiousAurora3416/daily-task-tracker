# 每日任务管理与提醒工具 — 产品需求文档（PRD）

| 字段 | 内容 |
|------|------|
| 产品名称 | 每日任务管理（Daily Task Tracker） |
| 文档版本 | v1.0 |
| 当前阶段 | MVP 已上线，规划 v1.1 ~ v2.0 |
| 更新日期 | 2026-08-08 |
| 负责人 | — |
| 技术栈 | Python 3.9+ / PySide6 (Qt6) / SQLite |

---

## 一、产品背景

### 1.1 现状与痛点

市面上的任务管理工具存在三类典型问题：

1. **重型化** — Todoist / TickTick / Notion 等需要登录账号、依赖网络、界面复杂，对「只想记下今天要做几件事」的用户是过度设计。
2. ** Electron 内存爆炸** — Pomotroid、Super Productivity 等 Electron 应用基础内存 ≥ 150MB，后台常驻吃资源。
3. **功能割裂** — 番茄钟应用（如 Pomodoro Timer）只管计时不管任务；任务应用不管专注计时；提醒功能分散在日历软件里。

用户希望有一个**轻量、本地、一体化的桌面小工具**，把「记任务 + 定时提醒 + 专注计时 + 看进度」整合到一起，常驻系统托盘，需要时呼出，不需要时安静待命。

### 1.2 解决方案

基于 Python + PySide6 + SQLite 构建一款**单进程、本地存储、原生系统托盘**的桌面应用：

- 启动后常驻托盘，关闭主窗口不退出
- 三种任务类型一体化：待办事项 / 习惯打卡 / 番茄专注
- 定时提醒按 HH:MM + 重复周期触发，跨日自动重置
- 全部数据 SQLite 本地存储，零网络依赖

---

## 二、目标用户

### 2.1 用户画像

| 画像 | 特征 | 核心诉求 |
|------|------|----------|
| **求职者** | 每天投递简历、刷行测题、准备面试 | 固定时间提醒投递；记录每日投递数量；专注刷题计时 |
| **学生 / 考证人群** | 长周期备考，需要养成习惯 | 习惯打卡；连续天数统计；番茄专注 |
| **独立开发者 / 知识工作者** | 多项目并行，容易分心 | 任务分类管理；番茄钟防分心；专注时长统计 |
| **轻量任务管理偏好者** | 拒绝臃肿工具，拒绝注册登录 | 本地存储、即开即用、零账号 |

### 2.2 非目标用户

- 团队协作场景（本项目不做多人协同）
- 企业级项目管理（不做甘特图、看板、工时审批）
- 移动端用户（仅桌面端）

---

## 三、产品目标

### 3.1 北极星指标

**周活跃天数（WAU 中一周内打开应用 ≥ 3 天的用户占比）** — 反映工具是否真正融入用户日常。

### 3.2 v1.0 MVP 目标（已达成）

| 目标 | 衡量标准 | 现状 |
|------|----------|------|
| 功能完整 | 任务 CRUD / 番茄钟 / 提醒 / 分类 / 统计全部可用 | ✅ |
| 轻量 | 运行内存 < 120MB（裸 Python 运行） | ✅ 实测 ~100MB |
| 离线可用 | 无任何网络依赖 | ✅ |
| 数据安全 | SQLite 单文件，WAL 模式 | ✅ |

### 3.3 v2.0 目标（规划中）

| 目标 | 衡量标准 |
|------|----------|
| 极轻量 | PyInstaller 打包后运行内存 < 60MB |
| 可分发 | 提供 macOS / Windows 一键安装包 |
| 可视化进度 | 7 天趋势图、完成率饼图 |
| 体验精进 | 浮动番茄钟、托盘图标实时倒计时 |

---

## 四、功能需求

### 4.1 功能架构

```
每日任务管理工具
├── 任务管理
│   ├── 任务 CRUD（新增 / 编辑 / 删除 / 启用停用）
│   ├── 三种类型：待办 / 打卡 / 专注
│   └── 今日任务视图（按重复周期过滤）
├── 分类管理
│   ├── 分类 CRUD + 颜色
│   └── 按分类筛选任务
├── 提醒系统
│   ├── 定时提醒（HH:MM + 重复周期）
│   ├── 系统托盘通知
│   └── 跨日自动重置
├── 番茄钟
│   ├── 工作 / 休息循环
│   ├── 自定义时长
│   └── 完成自动记录专注时长
├── 统计概览
│   ├── 今日完成率 / 专注时长 / 连续打卡
│   └── 近 7 天趋势
└── 系统集成
    ├── 系统托盘常驻
    └── 关闭窗口后台运行
```

### 4.2 功能清单与优先级

| ID | 功能 | 描述 | 优先级 | 版本 |
|----|------|------|--------|------|
| F01 | 任务新增 | 通过对话框创建任务，含类型/分类/提醒/重复/时长 | P0 | v1.0 ✅ |
| F02 | 任务编辑 | 双击或编辑按钮修改任务 | P0 | v1.0 ✅ |
| F03 | 任务删除 | 删除任务（含确认弹窗） | P0 | v1.0 ✅ |
| F04 | 任务完成 | 标记完成 / 撤销完成 | P0 | v1.0 ✅ |
| F05 | 今日任务视图 | 按重复周期过滤今日应完成任务 | P0 | v1.0 ✅ |
| F06 | 分类管理 | 增删改分类，自定义颜色 | P1 | v1.0 ✅ |
| F07 | 按分类筛选 | 侧栏点击分类筛选任务 | P1 | v1.0 ✅ |
| F08 | 定时提醒 | 到点触发系统通知 | P0 | v1.0 ✅ |
| F09 | 跨日重置 | 跨过 00:00 自动刷新任务状态 | P0 | v1.0 ✅ |
| F10 | 番茄钟计时 | 25 分钟工作 + 5 分钟休息循环 | P0 | v1.0 ✅ |
| F11 | 番茄钟自定义 | 按任务设置专注时长 | P1 | v1.0 ✅ |
| F12 | 专注记录 | 番茄钟完成自动写入日志 | P0 | v1.0 ✅ |
| F13 | 统计概览 | 完成率 / 专注时长 / 连续打卡 | P1 | v1.0 ✅ |
| F14 | 7 天趋势 | 近 7 天完成数 + 专注分钟列表 | P2 | v1.0 ✅ |
| F15 | 系统托盘 | 常驻托盘 + 右键菜单 + 通知 | P0 | v1.0 ✅ |
| F16 | 后台运行 | 关闭窗口不退出 | P0 | v1.0 ✅ |
| F17 | 优先级 | 任务支持普通/重要/紧急三级 | P1 | v1.1 |
| F18 | 子任务 | 一级嵌套子任务 | P2 | v1.1 |
| F19 | 暗色主题 | 跟随系统 / 手动切换 | P1 | v1.1 |
| F20 | 浮动番茄钟 | 置顶小窗番茄钟 | P2 | v1.2 |
| F21 | 托盘倒计时 | 托盘图标实时显示剩余分钟 | P2 | v1.2 |
| F22 | 提醒音效 | 到点播放提示音 | P2 | v1.2 |
| F23 | 数据导入导出 | JSON / CSV 备份恢复 | P1 | v1.2 |
| F24 | 统计图表 | 饼图 / 柱状图可视化 | P2 | v2.0 |
| F25 | 打包分发 | macOS .dmg / Windows .exe | P0 | v2.0 |
| F26 | 自启动 | 开机自启动选项 | P2 | v2.0 |
| F27 | 快捷键 | 全局快捷键呼出 / 开始番茄钟 | P2 | v2.0 |

### 4.3 核心用例

#### UC-01 添加每日提醒任务

**前置条件**：应用已启动并常驻托盘
**主流程**：
1. 用户点击托盘图标 → 主窗口弹出
2. 点击「＋ 添加任务」
3. 填写：标题「投递简历」、类型「待办」、分类「求职」
4. 勾选「定时提醒」→ 选择 10:00
5. 重复周期勾选「一 二 三 四 五」
6. 点击保存
**预期结果**：任务出现在今日列表，每个工作日 10:00 弹出托盘通知

#### UC-02 番茄专注

**前置条件**：已有「专注写代码」类型为「专注」的任务
**主流程**：
1. 选中该任务
2. 点击番茄钟区「开始工作」
3. 计时器从任务设置的时长（如 50 分钟）开始倒数
4. 倒数结束 → 弹出完成提示 → 自动记录 50 分钟专注时长到该任务
5. 用户可继续「开始休息」5 分钟
**预期结果**：今日专注时长 +50 分钟，任务完成数 +1

#### UC-03 查看进度

**主流程**：
1. 打开主窗口 → 右侧统计栏显示：完成率 60%、已完成 3/5、今日专注 2.5h、连续打卡 3 天
2. 下方列表展示近 7 天每日完成数与专注时长
**预期结果**：用户直观感知今日和近期投入情况

---

## 五、非功能性需求

### 5.1 性能

| 指标 | 目标 | 当前 |
|------|------|------|
| 启动时间 | < 1 秒 | ~1 秒（裸 Python） |
| 空闲内存 | < 60MB | ~100MB（v2.0 打包后优化） |
| 运行内存 | < 80MB | ~100MB |
| CPU 空闲占用 | < 1% | < 1%（QTimer 单次触发） |
| 数据库体积 | < 1MB（一年内） | < 100KB |

### 5.2 兼容性

| 平台 | 支持版本 | 状态 |
|------|----------|------|
| macOS | 13.0+ (Ventura) | ✅ 已验证 |
| Windows | 10 / 11 | ⏳ 待打包验证 |
| Linux | X11 / Wayland | ⏳ 待验证 |

### 5.3 数据安全

- 全部数据本地 SQLite 存储，**不上传任何用户数据**
- WAL 模式保证写入原子性，异常断电不丢数据
- v1.2 起支持手动导出 JSON 备份

### 5.4 可维护性

- 模块分层：`core`（业务逻辑）/ `ui`（界面）/ `utils`（工具）
- 数据层与 UI 层解耦，便于更换前端（如未来迁移到 QML）
- 单文件数据库便于备份迁移

---

## 六、信息架构与交互

### 6.1 主窗口三栏布局

```
┌──────────────────────────────────────────────────┐
│ 今日任务           │ 分类筛选   │ 统计概览        │
│ ─────────────────  │ ─────────  │ ────────────── │
│ ✅ [求职/待办] ... │ 全部       │ 完成率 60%      │
│ ▢  [学习/打卡] ... │ 求职       │ 已完成 3 / 5    │
│ ▢  [健康/待办] ... │ 学习       │ 今日专注 2.5h   │
│                    │ 健康       │ 连续打卡 3 天   │
│ [+ 添加] [完成] ...│ 未分类     │ ────────────── │
│                    │ [管理分类] │ 近 7 天         │
│ ┌─ 专注番茄钟 ───┐ │            │ 08-08 完成 3   │
│ │   25:00        │ │            │ 08-07 完成 5   │
│ │  工作中 · 写代码│ │            │ ...            │
│ │ [开始][休息][停]│ │            │                │
│ └────────────────┘ │            │                │
└──────────────────────────────────────────────────┘
```

### 6.2 系统托盘行为

| 操作 | 行为 |
|------|------|
| 单击 / 双击托盘图标 | 显示主窗口 |
| 右键托盘图标 | 弹出菜单：打开主窗口 / 添加任务 / 今日统计 / 退出 |
| 关闭主窗口 | 隐藏到托盘，应用继续运行 |
| 到点提醒 | 弹出系统通知（标题 + 任务列表） |
| 悬停托盘图标 | Tooltip 显示「今日 X/Y」 |

### 6.3 任务编辑对话框

字段：任务名称（必填）/ 任务类型（单选）/ 所属分类（下拉）/ 定时提醒（复选 + 时间）/ 重复周期（周一到周日复选）/ 专注时长（数字输入）

### 6.4 任务类型语义

| 类型 | 标识 | 用途 | 行为差异 |
|------|------|------|----------|
| 待办 | todo | 一次性或周期性事项 | 完成/撤销完成 |
| 打卡 | habit | 习惯养成（刷题、运动） | 每日打卡，连续天数统计 |
| 专注 | pomodoro | 需要专注计时的工作 | 可启动番茄钟并记录时长 |

---

## 七、数据模型

### 7.1 核心实体

```
Task
├── id, title
├── category (关联 Category.name)
├── type (todo / habit / pomodoro)
├── reminder_time (HH:MM, 可空)
├── repeat_days (JSON 数组 [1..7], ISO 周几)
├── duration (番茄钟时长, 分钟)
├── is_enabled
└── created_at, updated_at

Category
├── id, name (唯一)
└── color (#RRGGBB)

TaskLog
├── id, task_id (外键)
├── completed_at (时间戳)
└── duration_minutes (专注时长, 0 表示普通完成)
```

### 7.2 统计派生

- **今日完成率** = `今日已完成任务数 / 今日任务总数`
- **今日专注时长** = `SUM(task_logs.duration_minutes WHERE 当日)`
- **连续打卡天数** = 从今日倒推，连续 `done > 0` 的天数
- **7 天趋势** = 近 7 天每日 done 与 focus_minutes 数组

---

## 八、里程碑与路线图

| 版本 | 主题 | 核心交付 | 状态 |
|------|------|----------|------|
| **v1.0** | MVP | 任务管理 + 番茄钟 + 提醒 + 分类 + 统计 + 托盘 | ✅ 已完成 |
| **v1.1** | 体验增强 | 优先级、子任务、暗色主题、提醒音效 | 🔜 规划 |
| **v1.2** | 可视化与备份 | 浮动番茄钟、托盘倒计时、JSON 导入导出、饼图 | 🔜 规划 |
| **v2.0** | 分发就绪 | PyInstaller 打包、macOS .dmg / Windows .exe、开机自启、全局快捷键 | 🔜 规划 |

---

## 九、度量指标

### 9.1 产品指标

| 指标 | 定义 | 目标 |
|------|------|------|
| 周活跃天数 | 一周内打开应用 ≥ 3 天 | ≥ 4 天 |
| 日均任务创建数 | 用户每天新增任务数 | ≥ 3 |
| 番茄钟使用率 | 活跃用户中使用番茄钟的占比 | ≥ 50% |
| 提醒触达率 | 触发提醒后 5 分钟内打开主窗口的比例 | ≥ 30% |

### 9.2 技术指标

| 指标 | 目标 |
|------|------|
| 启动崩溃率 | < 0.1% |
| 内存占用（v2.0 打包后） | < 60MB |
| 数据库异常率 | 0 |

---

## 十、风险与约束

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| PySide6 体积过大 | 安装包偏大、内存偏高 | v2.0 切换 PySide6-Essentials + PyInstaller 排除未用模块 |
| macOS 通知权限 | 用户拒绝通知权限则提醒失效 | 首次启动引导授权，文档说明降级方案 |
| 跨平台托盘差异 | Linux 部分桌面环境托盘支持不佳 | 文档标注推荐 GNOME Shell + AppIndicator |
| 数据丢失 | SQLite 文件损坏 | v1.2 加入自动备份机制 |
| Python 运行时依赖 | 用户需装 Python | v2.0 打包为独立可执行文件 |

---

## 十一、附录

### 11.1 术语表

| 术语 | 含义 |
|------|------|
| 番茄钟 | Pomodoro Technique，25 分钟专注 + 5 分钟休息的循环工作法 |
| 重复周期 | 任务按周几重复触发，ISO 周几（1=周一 … 7=周日） |
| 打卡 | 习惯类任务，每日标记完成，用于养成习惯 |
| Streak | 连续打卡天数，从今日倒推连续完成的天数 |
| WAL | SQLite Write-Ahead Logging，提升并发读写性能的模式 |

### 11.2 竞品参考

| 项目 | 借鉴点 |
|------|--------|
| [chaubeysatyam/Pomodoro-Timer (Routine Pro)](https://github.com/chaubeysatyam/Pomodoro-Timer) | 浮动番茄钟、优先级、CSV 导入导出 |
| [Yxianshe/Manage-Myday](https://github.com/Yxianshe/Manage-Myday) | 暗色主题 QSS、饼图统计、悬浮便签模式 |
| [Pomodoro Tray Timer](https://juejin.cn/post/7657133810313969700) | 托盘图标实时绘制剩余分钟数 |
| [amraoui-mo7amed/pomcraft](https://github.com/amraoui-mo7amed/pomcraft) | 任务与番茄钟会话绑定 |

### 11.3 相关文档

- [design_doc.md](./design_doc.md) — 技术实现方案
- [requirements.txt](./requirements.txt) — 依赖列表
